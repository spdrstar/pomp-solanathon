import { Currency } from "@thirdweb-dev/sdk/node_modules/@bundlr-network/client/build/common/types";

export interface WebCurrency extends Currency {
    getPublicKey(): Promise<string | Buffer>
    ready(): Promise<void>
}


